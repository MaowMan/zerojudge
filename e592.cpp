#include <iostream>
#include <vector>
#include <string>

int main(){
    int n;
    std::cin>>n;
    for(int i=0;i<n;i++){
        int k;
        std::cin>>k;
        std::vector<std::string> names(k);
        for(int r=0;r<k;r++){
            std::string str;
            names[r]=std::getline(std::cin,str);
        }
        std::vector<std::vector <int> > seq()
        for(int r=0;r<k;r++)

    }
}
